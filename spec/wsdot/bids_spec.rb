require "spec_helper"

RSpec.describe Wsdot::Bids do
  it "has a version number" do
    expect(Wsdot::Bids::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
